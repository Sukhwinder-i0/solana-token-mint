import type { NextConfig } from "next";
// import { nodePollyfills } from "next-plugin-node-polyfills";

const nextConfig: NextConfig = {
  /* config options here */
  // plugins: [
  //   nodePolyfills(),
  // ],
};

export default nextConfig;
